# 자동 생성 API 테스트 케이스

- 테스트 대상 Base URL: `https://petstore.swagger.io/v2`
- 명세 파일 경로: `D:/jhseo/project/swagger-api/swagger/swagger.yaml`
- 명세 원본 위치: `D:\jhseo\project\swagger-api\swagger\swagger.yaml`

## /pet/{petId}/uploadImage [POST]
- 요약: uploads an image
- 성공 기대값: 문서에 정의된 `200` 응답이 반환되어야 한다.
- 파라미터 처리: 문서화된 path 또는 query 파라미터를 정상 수용해야 한다.
- 필수값 검증: 문서에 정의된 필수 파라미터는 반드시 강제되어야 한다.
- 인증: 인증 정보가 없거나 잘못되면 문서의 보안 스키마에 맞게 실패해야 한다.

## /pet [POST]
- 요약: Add a new pet to the store
- 성공 기대값: 문서에 정의된 `200` 응답이 반환되어야 한다.
- 파라미터 처리: 문서화된 path 또는 query 파라미터를 정상 수용해야 한다.
- 필수값 검증: 문서에 정의된 필수 파라미터는 반드시 강제되어야 한다.
- 인증: 인증 정보가 없거나 잘못되면 문서의 보안 스키마에 맞게 실패해야 한다.

## /pet [PUT]
- 요약: Update an existing pet
- 성공 기대값: 문서에 정의된 `200` 응답이 반환되어야 한다.
- 파라미터 처리: 문서화된 path 또는 query 파라미터를 정상 수용해야 한다.
- 필수값 검증: 문서에 정의된 필수 파라미터는 반드시 강제되어야 한다.
- 인증: 인증 정보가 없거나 잘못되면 문서의 보안 스키마에 맞게 실패해야 한다.

## /pet/findByStatus [GET]
- 요약: Finds Pets by status
- 성공 기대값: 문서에 정의된 `200` 응답이 반환되어야 한다.
- 파라미터 처리: 문서화된 path 또는 query 파라미터를 정상 수용해야 한다.
- 필수값 검증: 문서에 정의된 필수 파라미터는 반드시 강제되어야 한다.
- 인증: 인증 정보가 없거나 잘못되면 문서의 보안 스키마에 맞게 실패해야 한다.

## /pet/findByTags [GET]
- 요약: Finds Pets by tags
- 성공 기대값: 문서에 정의된 `200` 응답이 반환되어야 한다.
- 파라미터 처리: 문서화된 path 또는 query 파라미터를 정상 수용해야 한다.
- 필수값 검증: 문서에 정의된 필수 파라미터는 반드시 강제되어야 한다.
- 인증: 인증 정보가 없거나 잘못되면 문서의 보안 스키마에 맞게 실패해야 한다.

## /pet/{petId} [GET]
- 요약: Find pet by ID
- 성공 기대값: 문서에 정의된 `200` 응답이 반환되어야 한다.
- 파라미터 처리: 문서화된 path 또는 query 파라미터를 정상 수용해야 한다.
- 필수값 검증: 문서에 정의된 필수 파라미터는 반드시 강제되어야 한다.
- 리소스 미존재: 존재하지 않는 식별자는 문서에 정의된 `404` 동작을 따라야 한다.
- 인증: 인증 정보가 없거나 잘못되면 문서의 보안 스키마에 맞게 실패해야 한다.

## /pet/{petId} [POST]
- 요약: Updates a pet in the store with form data
- 성공 기대값: 문서에 정의된 `200` 응답이 반환되어야 한다.
- 파라미터 처리: 문서화된 path 또는 query 파라미터를 정상 수용해야 한다.
- 필수값 검증: 문서에 정의된 필수 파라미터는 반드시 강제되어야 한다.
- 인증: 인증 정보가 없거나 잘못되면 문서의 보안 스키마에 맞게 실패해야 한다.

## /pet/{petId} [DELETE]
- 요약: Deletes a pet
- 성공 기대값: 문서에 정의된 `200` 응답이 반환되어야 한다.
- 파라미터 처리: 문서화된 path 또는 query 파라미터를 정상 수용해야 한다.
- 필수값 검증: 문서에 정의된 필수 파라미터는 반드시 강제되어야 한다.
- 리소스 미존재: 존재하지 않는 식별자는 문서에 정의된 `404` 동작을 따라야 한다.
- 인증: 인증 정보가 없거나 잘못되면 문서의 보안 스키마에 맞게 실패해야 한다.

## /store/inventory [GET]
- 요약: Returns pet inventories by status
- 성공 기대값: 문서에 정의된 `200` 응답이 반환되어야 한다.
- 인증: 인증 정보가 없거나 잘못되면 문서의 보안 스키마에 맞게 실패해야 한다.

## /store/order [POST]
- 요약: Place an order for a pet
- 성공 기대값: 문서에 정의된 `200` 응답이 반환되어야 한다.
- 파라미터 처리: 문서화된 path 또는 query 파라미터를 정상 수용해야 한다.
- 필수값 검증: 문서에 정의된 필수 파라미터는 반드시 강제되어야 한다.

## /store/order/{orderId} [GET]
- 요약: Find purchase order by ID
- 성공 기대값: 문서에 정의된 `200` 응답이 반환되어야 한다.
- 파라미터 처리: 문서화된 path 또는 query 파라미터를 정상 수용해야 한다.
- 필수값 검증: 문서에 정의된 필수 파라미터는 반드시 강제되어야 한다.
- 리소스 미존재: 존재하지 않는 식별자는 문서에 정의된 `404` 동작을 따라야 한다.

## /store/order/{orderId} [DELETE]
- 요약: Delete purchase order by ID
- 성공 기대값: 문서에 정의된 `200` 응답이 반환되어야 한다.
- 파라미터 처리: 문서화된 path 또는 query 파라미터를 정상 수용해야 한다.
- 필수값 검증: 문서에 정의된 필수 파라미터는 반드시 강제되어야 한다.
- 리소스 미존재: 존재하지 않는 식별자는 문서에 정의된 `404` 동작을 따라야 한다.

## /user/createWithList [POST]
- 요약: Creates list of users with given input array
- 성공 기대값: 문서에 정의된 `200` 응답이 반환되어야 한다.
- 파라미터 처리: 문서화된 path 또는 query 파라미터를 정상 수용해야 한다.
- 필수값 검증: 문서에 정의된 필수 파라미터는 반드시 강제되어야 한다.

## /user/{username} [GET]
- 요약: Get user by user name
- 성공 기대값: 문서에 정의된 `200` 응답이 반환되어야 한다.
- 파라미터 처리: 문서화된 path 또는 query 파라미터를 정상 수용해야 한다.
- 필수값 검증: 문서에 정의된 필수 파라미터는 반드시 강제되어야 한다.
- 리소스 미존재: 존재하지 않는 식별자는 문서에 정의된 `404` 동작을 따라야 한다.

## /user/{username} [PUT]
- 요약: Updated user
- 성공 기대값: 문서에 정의된 `200` 응답이 반환되어야 한다.
- 파라미터 처리: 문서화된 path 또는 query 파라미터를 정상 수용해야 한다.
- 필수값 검증: 문서에 정의된 필수 파라미터는 반드시 강제되어야 한다.
- 리소스 미존재: 존재하지 않는 식별자는 문서에 정의된 `404` 동작을 따라야 한다.

## /user/{username} [DELETE]
- 요약: Delete user
- 성공 기대값: 문서에 정의된 `200` 응답이 반환되어야 한다.
- 파라미터 처리: 문서화된 path 또는 query 파라미터를 정상 수용해야 한다.
- 필수값 검증: 문서에 정의된 필수 파라미터는 반드시 강제되어야 한다.
- 리소스 미존재: 존재하지 않는 식별자는 문서에 정의된 `404` 동작을 따라야 한다.

## /user/login [GET]
- 요약: Logs user into the system
- 성공 기대값: 문서에 정의된 `200` 응답이 반환되어야 한다.
- 파라미터 처리: 문서화된 path 또는 query 파라미터를 정상 수용해야 한다.
- 필수값 검증: 문서에 정의된 필수 파라미터는 반드시 강제되어야 한다.

## /user/logout [GET]
- 요약: Logs out current logged in user session
- 성공 기대값: 문서에 정의된 `200` 응답이 반환되어야 한다.

## /user/createWithArray [POST]
- 요약: Creates list of users with given input array
- 성공 기대값: 문서에 정의된 `200` 응답이 반환되어야 한다.
- 파라미터 처리: 문서화된 path 또는 query 파라미터를 정상 수용해야 한다.
- 필수값 검증: 문서에 정의된 필수 파라미터는 반드시 강제되어야 한다.

## /user [POST]
- 요약: Create user
- 성공 기대값: 문서에 정의된 `200` 응답이 반환되어야 한다.
- 파라미터 처리: 문서화된 path 또는 query 파라미터를 정상 수용해야 한다.
- 필수값 검증: 문서에 정의된 필수 파라미터는 반드시 강제되어야 한다.
